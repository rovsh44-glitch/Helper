# Media Pack

This directory is the private-core manifest for curated public-showcase assets.

The actual published media pack lives in the separate showcase repository. Do not dump raw captures here by default just because they exist locally.

Recommended curated assets:

- `helper-evolution-dashboard.png`
- `helper-runtime-console.png`
- `helper-library-indexing.png`
- `helper-generated-chess-app.png`
- `helper-generated-calculator-app.png`

Rules:

- do not capture secrets, local paths, or private data
- prefer real product screenshots over mockups
- keep captions honest about what is and is not production-ready
- treat this directory as a manifest/source pointer unless an asset is explicitly approved for core-repo tracking
