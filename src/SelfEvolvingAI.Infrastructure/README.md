# SelfEvolvingAI.Infrastructure

This directory is a legacy placeholder from an earlier repository layout.

Current status:

- no active `.csproj` references this tree
- no runtime code is loaded from this location
- the directory is kept only to preserve historical path continuity while cleanup work is phased

Policy:

- do not add new code here without creating an explicit project and wiring it into the canonical solution policy
- if no concrete owner or build integration appears, remove this tree in a future cleanup pass
