# SelfEvolvingAI.Infrastructure.Core

This directory is a legacy placeholder from an earlier repository layout.

Current status:

- no active `.csproj` references this tree
- no production service registration resolves code from this location
- the remaining subfolders are historical artifacts, not a supported subsystem boundary

Policy:

- do not revive this tree implicitly through ad hoc files
- any future reintroduction must happen through a named project, tests, and explicit solution membership
